// Seed script for alternatives — run manually when needed.
// Usage: node scripts/seed-alternatives.mjs
//
// This script is now a reference template. New competitors should be
// added directly through Sanity Studio or via a one-off script.
//
// Schema expects flat f_ feature fields, verdict fields, and faqItems array.

console.log('Seed script is now a reference only.')
console.log('Add new competitors through Sanity Studio at /studio.')
console.log('Or create a one-off script following the flat schema format.')
