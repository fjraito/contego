export const metadata = {
  title: 'Pricing',
  description:
    'Contego pricing — three retainers built to compound. Month-to-month, no per-seat tax, with a full ROI model and feature-by-feature comparison.',
}

export default function PricingLayout({ children }) {
  return children
}
