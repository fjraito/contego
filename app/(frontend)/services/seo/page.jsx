import { ServicePageClient } from '@/components/ServicePageClient'

export const metadata = {
  title: 'SEO',
  description: 'Programmatic SEO for prop firms — topical authority, buyer-intent rankings, and outranking affiliates on every review and "best of" query.',
}

export default function SEOPage() {
  return <ServicePageClient slug="seo" />
}
